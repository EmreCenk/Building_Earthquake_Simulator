




void draw_vector(PVector origin, PVector vect){
  println("come on");
  println(origin.x, origin.y, origin.x + vect.x, origin.y +vect.y);
  println("yes");
  line(origin.x, origin.y, origin.x + vect.x, origin.y +vect.y);

}
